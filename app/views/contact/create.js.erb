var form = document.querySelectorAll('.remote-form')[0];

<% if @message.errors.any? %>
  form.innerHTML = "<%= j render 'contact/form' %>";
<% else %>
  form.reset();
  form.innerHTML = "<%= j render 'contact/thankyou' %>";
<% end %>

window.scrollTo(0,0);
