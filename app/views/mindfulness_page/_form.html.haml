= form_with model: @mindfulness_page, url: mindfulness_page_path, class: 'lmb' do |f|
  = form_title_for f.object
  = render 'application/forms/fieldset', f: f
  = render 'application/forms/submit_button', f: f
